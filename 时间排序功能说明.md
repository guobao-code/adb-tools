# 任务文件夹时间排序功能说明

## 🔧 修改内容

已成功修改 `video_downloader.py` 以支持基于 `task_running_time` 字段的**任务文件夹时间排序**。

## 📋 新增功能

### 1. **智能最早时间提取**
- 新增 `extract_task_earliest_time()` 函数
- 从任务的 `task_running_time` 字段中提取最早的视频时间
- 支持多种时间格式：
  - 时间字符串数组：`["09:15:30", "09:20:45", "09:25:10"]`
  - 时间戳数组：`[1663445730, 1663445790, 1663445850]`
  - 单个时间值：`"09:15:30"` 或 `1663445730`

### 2. **任务文件夹排序**
- **文件夹**现在按任务内最早视频时间排序
- 文件夹命名包含最早视频时间：`001_20250718_091530_篮球_task123`
- 确保文件夹本身按时间顺序排列

### 3. **任务时间显示**
- 日志显示每个任务的最早视频时间
- 格式：`任务按最早视频时间顺序排列`

## 🎯 工作原理

### 排序逻辑：
```python
# 1. 为每个任务计算最早视频时间
for task in tasks:
    earliest_time = extract_task_earliest_time(task)
    task["video_earliest_time"] = earliest_time

# 2. 按最早视频时间排序任务
tasks.sort(key=lambda x: x["video_earliest_time"])

# 3. 文件夹使用最早时间命名
time_str = task_time.strftime("%Y%m%d_%H%M%S")
task_folder = f"{task_idx:03d}_{time_str}_{sport_name}_{task_id}"
```

### 最早时间提取优先级：
1. **task_running_time** (最高优先级)
   - 如果是数组，取最早的时间值
   - 如果是单个值，直接使用该值
2. **其他时间字段**：
   - video_time, start_time, end_time, record_time 等
3. **任务原始时间** (后备方案)

## 📁 文件结构示例

修改前：
```
001_20250718_090000_篮球_task123/    (基于任务创建时间)
002_20250718_100000_足球_task456/
003_20250718_110000_游泳_task789/
```

修改后：
```
001_20250718_091530_篮球_task123/    (基于最早视频 09:15:30)
002_20250718_092045_足球_task456/    (基于最早视频 09:20:45)
003_20250718_091510_游泳_task789/    (基于最早视频 09:15:10)
```

## 🚀 使用方法

运行方式与之前完全相同：
```bash
python video_downloader.py
```

程序会自动：
1. 解析每个任务的 `task_running_time` 字段
2. 计算每个任务的最早视频时间
3. 按最早视频时间排序任务文件夹
4. 在日志中显示排序信息

## ✅ 兼容性

- 向后兼容：如果没有 `task_running_time` 字段，使用任务原始时间
- 容错处理：时间解析失败时使用默认值
- 多格式支持：自动识别不同的时间格式

## 📊 日志输出示例

```
任务按最早视频时间顺序排列:
  01. 2025-07-18 09:15:30 - 篮球 (ID: task123)
  02. 2025-07-18 09:20:45 - 足球 (ID: task456) 
  03. 2025-07-18 09:15:10 - 游泳 (ID: task789)
```

## 💡 核心变化

**重点**：现在**文件夹的排列顺序**是基于每个任务内部视频的最早时间，而不是任务本身的创建时间。这样确保了文件夹的时间顺序反映了实际视频内容的时间顺序。